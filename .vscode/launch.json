{
    "configurations": [
        {
            "name": "Docker .NET Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "netCore": {
                "appProject": "${workspaceFolder}/src/Presentation/GradeBookMicroservice.WebHost/GradeBookMicroservice.WebHost.csproj"
            }
        }
    ]
}