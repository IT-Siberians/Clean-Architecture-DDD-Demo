apiVersion: v1
kind: Service
metadata:
  name: {{ tpl .Values.service.name . }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
  type: ClusterIP
  