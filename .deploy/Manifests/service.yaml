apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: volumes-lesson
spec:
  selector:
    app: web-application-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  